// https://medium.com/swlh/writing-a-parser-getting-started-44ba70bb6cc9

const peek = (k?: number) => {
  // next token
  // or next kth token
}

const consume = (k?: number) => {
  // next token
  // or next kth token
}
